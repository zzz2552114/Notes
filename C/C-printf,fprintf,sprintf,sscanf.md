# printf, fprintf, sprintf，sscanf



### 1. `printf`、`fprintf`、`sprintf` 的核心区别

为了让你一目了然，请看下表：

| 函数          | 输出目标（目的地）              | 核心参数特征                         | 典型使用场景                                                 |
| :------------ | :------------------------------ | :----------------------------------- | :----------------------------------------------------------- |
| **`printf`**  | **标准输出流（`stdout`）**      | `printf(格式, 变量...)`              | 普通的程序运行结果输出，显示给用户看。                       |
| **`fprintf`** | **指定的文件流（`FILE*`）**     | `fprintf(文件流指针, 格式, 变量...)` | 写入文件、写入标准错误（`stderr`）、写入网络套接字等。**自由度最高**。 |
| **`sprintf`** | **内存中的字符数组（`char*`）** | `sprintf(缓冲区, 格式, 变量...)`     | 将数据格式化拼接成一个字符串，方便后续存储或传输。           |

------

### 2. 深入举例说明

- **`printf`**：直接打印到屏幕（通常指标准输出）。

  ```c
  printf("Hello %d", 123); // 屏幕上显示：Hello 123
  ```

  

- **`fprintf`**：你可以指定任何文件，包括屏幕（通过 `stderr`）或磁盘文件。

  ```c
  // 你的代码：打印错误到屏幕（但用的是错误输出通道）
  fprintf(stderr, "Usage: ..."); 
  
  // 写入磁盘文件
  FILE *fp = fopen("log.txt", "w");
  fprintf(fp, "写入文件的数据 %d", 456); // 写入到 log.txt，屏幕不显示
  ```

  

- **`sprintf`**：不打印，也不写文件，而是把结果放入你给的变量内存里。

  ```c
  char buffer[50];
  sprintf(buffer, "Score: %d", 100);
  // 此时 buffer 数组中存储的内容是 "Score: 100"，屏幕上看不见任何输出
  printf("%s", buffer); // 如果你想看，还得再调用 printf
  ```

  

------

### 3. 特别注意（避坑指南）

- **安全性**：**`sprintf` 极其危险**，因为它不检查目标缓冲区大小，如果格式化后的字符串过长，会**溢出**覆盖其他内存，导致程序崩溃或安全漏洞。在实际工程中，**建议使用更安全的 `snprintf`**（允许指定最大写入长度）。
- **返回值**：这三个函数都返回一个 `int` 值，表示**成功写入的字符总数**（不包括结尾的 `'\0'`）。你可以利用这个返回值来做长度校验，例如 `int len = printf("Hi");` 此时 `len` 等于 2。

**总结一句话**：`printf` 是默认输出，`fprintf` 是自由指定输出地（错误信息必须用它配合 `stderr`），`sprintf` 是输出到内存字符串（注意防溢出）。

---

### 

### sscanf

**示例**

```c
char line[] = "L 0x400000,256";
sscanf(line, " %c %lx,%d", &op, &addr, &size);
```

#### 参数1：`line`（输入源）

这是源字符串。假设 `line` 里面存着 `"I 0x7fff1234,1024"`。`sscanf` 就像一把扫描仪，对着这个字符串开始读。



#### 参数2：`" %c %lx,%d"`（格式控制——匹配规则）

这是告诉扫描仪“你要按什么规则去切分这个字符串”。我们把它拆成碎片看：

- **` `（前导空格）**：跳过 `line` 开头可能存在的所有空白字符（空格、换行、Tab）。鲁棒性很强。
- **`%c`**：读取**一个字符**（不跳过空格，但因为前面有空格跳过符，所以它读到的是第一个非空白字符 `'I'`）。这个字符被存进 `op`。
- **` `（中间空格）**：匹配并跳过 `'I'` 后面的空白字符。
- **`%lx`**：这是重点。`l` 是长度修饰符（表示 `long`），`x` 表示十六进制数。它会读取像 `7fff1234` 这样的十六进制字符，直到遇到非十六进制字符（比如逗号 `,`）。解析后的数值存入 `addr`（类型是 `unsigned long`）。
- **`,`**：**逐字匹配逗号**。它会吃掉字符串中间的逗号 `,`。
- **`%d`**：读取一个十进制整数，存入 `size`。
- 

#### 参数3：`&op, &addr, &size`（目标“写入”位置）

注意这里都带了 **`&`（取地址符）**，这里是**写到内存变量里**。
`sscanf` 拿到这些地址后，会把解析出来的 `'I'` 写入 `op` 的内存格子，把数字写入 `addr` 和 `size` 的内存格子。



#### 返回值： `== 3` 是什么意思？

`sscanf` 的返回值是 **“成功匹配并赋值的参数个数”**。

- 格式串里要求匹配 3 个东西（`%c`, `%lx`, `%d`）。
- 如果 `line` 格式完全正确，返回 `3`，进入 `if` 分支。
- 如果 `line` 是空串或者少了个逗号，比如 `"I 0x1234"`，那么 `%d` 匹配失败，函数返回 `2`（或者更少），不进入分支，避免了你用未初始化的 `size` 去操作，**保证安全**。

---

### 全流程表格

```c
char line[] = "L 0x400000,256";
sscanf(line, " %c %lx,%d", &op, &addr, &size);
```

执行 `sscanf` 时，内存中发生的事情如下：

| 步骤 | 指针在 `line` 中扫描 | 匹配动作 | 写入哪里                       |
| :--- | :------------------- | :------- | :----------------------------- |
| 1    | 跳过开头空格（没有） | ``       | 无                             |
| 2    | 读到 `'L'`           | `%c`     | 写入 `op`（此时 `op` = `'L'`） |
| 3    | 跳过 `'L'` 后的空格  | ``       | 无                             |
| 4    | 读到 `0x400000`      | `%lx`    | 转为数值 4194304，写入 `addr`  |
| 5    | 匹配中间的逗号 `,`   | `,`      | 消耗掉逗号                     |
| 6    | 读到 `256`           | `%d`     | 转为数值 256，写入 `size`      |

最终：

- `op` 变成 `'L'`（表示 Load 加载指令）
- `addr` 变成 `4194304`
- `size` 变成 `256`

------

### 5. 一个容易踩的坑（额外提醒）

格式串中的 **`%c`** 很特殊。如果你写成 `"%c %lx,%d"`（前导没有空格），而 `line` 开头有换行符，`%c` 会把换行符 `\n` 读进 `op`，导致逻辑错误。
***所以代码里 格式串 开头有个空格是非常专业、非常严谨的写法***，就是为了吃掉 `line` 前面可能残留的 `\n` 或空格。

**一句话总结**：`sscanf` 是个**数据提取器**，`line` 是它的**提取对象**（来源），`&变量` 是它的**存放位置**（归宿）。它绝不修改 `line` 字符串本身，只从里面“抠”数据出来喂给变量。