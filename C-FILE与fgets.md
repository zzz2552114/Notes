## 一、C 语言文件系统与 *FILE

### `fopen` 背后的原理——从“方便”到“内核”

`fopen`是C标准库提供的文件操作函数，它的工作远不止“打开文件”那么简单。它的底层原理可以分为三层：

#### 第一层：C库函数（`fopen`）

你调用`fopen("test.txt", "w")`。这是C标准库提供的一个**方便、可移植的接口**。

#### 第二层：系统调用（`open`）

`fopen`内部会调用操作系统提供的**系统调用（System Call）**，在Linux中就是 **`open`**。`open`是内核提供的服务，用来真正地操作文件。

#### 第三层：内核操作

`open`系统调用被触发后，CPU会从用户态切换到内核态，由内核来执行真正的打开操作：

1. **寻找文件**：内核根据你给的路径`test.txt`，在文件系统中找到这个文件（或准备创建它）。
2. **创建内核对象**：内核会在内存中创建一个**内核级别的文件对象**（也叫`struct file`），用来描述这个被打开的文件（包含文件位置、大小、属性等）。
3. **分配文件描述符**：内核会在这个进程的**文件描述符表**中找到一个空闲的位置，并返回一个**非负整数**作为该文件的**文件描述符（File Descriptor, fd）**。这个`fd`就像一个“房间号”，是进程后续操作这个文件的凭证。

#### 第四层：C语言：回到 `fopen`

内核的`open`调用返回一个整数`fd`。C库的`fopen`拿到这个`fd`后，会做两件重要的事：

1. ***分配`FILE`结构体***：在用户态的内存中，分配一个 **`FILE`结构体**（我们常说的`FILE*`指针指向的就是它）。
2. **封装`fd`和缓冲区**：将这个`fd`存放到`FILE`结构体中，并**在`FILE`结构体里开辟一块缓冲区**。我们之前讨论的所有缓冲行为，都发生在这个由`FILE`管理的用户态缓冲区里。

最终，`fopen`将这个封装好的`FILE`结构体的地址返回给你。

---



## 二、逐行处理文件

在 C 语言中逐行处理文件，最常用且标准的方法是使用 **`fgets()`** 或 POSIX 提供的 **`getline()`**。下面我会详细讲解这两种方式，并结合你的缓存模拟器场景，给出完整的代码示例。

---

### 1. 基本步骤

无论用哪种函数，流程都是固定的：

1. **用 `fopen()` 打开文件**，得到 `FILE *` 指针。
2. **循环读取每一行**，直到文件结束。
3. **处理当前行**（例如解析、计数等）。
4. **用 `fclose()` 关闭文件**。

---

### 2. 使用 `fgets()`（标准 C）

`fgets()` 原型：

```c
char *fgets(char *s, int size, FILE *stream);
```



- 从 `stream` 读取最多 `size-1` 个字符，存入 `s`，并自动添加 `'\0'`。
- 遇到换行符 `'\n'` 或 EOF 时停止。
- 成功返回 字符串首指针 `s`，失败或 EOF 返回 `NULL`。

**优点**：简单、可移植。
**缺点**：需要预先分配固定大小的缓冲区，若行太长会被截断。



***场景示例***

```c
FILE* fp = fopen("trace.txt","r");

char* line[50];
while(( fgets(line,50,fp)) != NULL){
    char op;
    unsigned long addr;
    int size;
    if (sscanf(line, " %c %lx,%d", &op, &addr, &size) == 3) {
        // 成功解析，根据 op 的值进行处理
    }
   }
```



其中：Valgrind trace 文件的每一行通常形如：

```
I 0400d7d4,2
 M 0421c7f0,4
 L 04f6b868,8
 S 7ff0005a8,4
```

格式为：`操作 地址,大小`
**可以用 `sscanf` 轻松解析：**



------

## 注意事项

- **换行符处理**：`fgets` 会保留行尾的 `\n`，解析时通常不影响 `sscanf`（因为 `%s` 等会跳过空白），但如果你要比较字符串，可能需要去掉。
- **空行**：如果文件有空行，`fgets` 仍会读入（只含 `\n`），要自行判断。
- **大文件**：逐行处理是内存友好的，不会一次性加载整个文件。
- **错误处理**：检查 `fopen` 返回值；对于 `fgets`，判断是否因错误而中断（可用 `feof` 和 `ferror`）。